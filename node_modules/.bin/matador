#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../matador/bin/matador.js" "$@"
  ret=$?
else 
  node  "$basedir/../matador/bin/matador.js" "$@"
  ret=$?
fi
exit $ret
